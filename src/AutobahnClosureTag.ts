/**
 * AutobahnClosureTag automatically generated by SDKgen please do not edit this file manually
 * {@link https://sdkgen.app}
 */

import {TagAbstract, HttpRequest} from "sdkgen-client"
import {ClientException, UnknownStatusCodeException} from "sdkgen-client";

import {AutobahnClosureCollection} from "./AutobahnClosureCollection";
import {Response} from "./Response";
import {ResponseException} from "./ResponseException";

export class AutobahnClosureTag extends TagAbstract {
    /**
     * Returns available closures for a specific autobahn
     *
     * @returns {Promise<AutobahnClosureCollection>}
     * @throws {ResponseException}
     * @throws {ClientException}
     */
    public async getAll(autobahnId: string): Promise<AutobahnClosureCollection> {
        const url = this.parser.url('/autobahn/:autobahn_id/closure', {
            'autobahn_id': autobahnId,
        });

        let request: HttpRequest = {
            url: url,
            method: 'GET',
            headers: {
            },
            params: this.parser.query({
            }, [
            ]),
        };

        const response = await this.httpClient.request(request);
        if (response.ok) {
            return await response.json() as AutobahnClosureCollection;
        }

        const statusCode = response.status;
        if (statusCode === 400) {
            throw new ResponseException(await response.json() as Response);
        }

        if (statusCode === 404) {
            throw new ResponseException(await response.json() as Response);
        }

        if (statusCode === 500) {
            throw new ResponseException(await response.json() as Response);
        }

        throw new UnknownStatusCodeException('The server returned an unknown status code: ' + statusCode);
    }



}
